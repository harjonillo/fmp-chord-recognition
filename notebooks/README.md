# Notebooks

Exploratory notebooks mirroring the FMP §5.2 walk-through, applied to the Harp
Étude.


1. **`01_chromagrams.ipynb`** — Load Op. 25 No. 1, compute STFT / IIR /
   CQT chromagrams, plot all three with reference annotations overlaid

2. **`02_template_recognition.ipynb`** — Run `recognize_template` on each
   variant, visualize the time–chord similarity matrix, look at where the
   argmax disagrees with the reference
